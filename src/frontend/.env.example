# Frontend environment variables
# Copy this file to .env and fill in your values — NEVER commit .env

# Override API base URL if the backend runs on a different host/port
# During local development the Vite dev-server proxy handles /api automatically.
VITE_API_BASE_URL=/api
